generate headers, given email

spf check (flag) {ip-addr} {mail-from} {helo}
dkim check (flag) {msg}, dnsfunc
dmarc (flag) {spf result | dkim result}, dnsfunc
auth results

dkim sig (flag) {message, selector, domain, privkey, identity=None, canonicalize=(b'relaxed', b'simple'), signature_algorithm=b'rsa-sha256', include_headers, length}
arc check + sig (flag) 


